# Service Configuration
HOST=0.0.0.0
PORT=8000
ENVIRONMENT=production

# Policy Settings
SECURITY_POLICY_MODE=balanced
SECURITY_FAIL_MODE=fail_closed

# Optional: Upstream LLM Proxy Credentials (if using /v1/chat/completions)
GROQ_API_KEY=
OPENAI_API_KEY=
